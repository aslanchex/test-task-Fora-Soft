// rendering chat room component

import React, { Component } from "react";

export default class ChatRoom extends Component {
  constructor(props) {
    super(props);

    this.state = {
      chatTitle: ""
    };
  }

  render() {
    return <div />;
  }
}
